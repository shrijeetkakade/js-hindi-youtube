"use strict";// treat all JS code as a newer version

// alert(3+3) // we are using node js not browser there is a different way for using alert in js

console.log(3+2); console.log("shrijeet")
/*
above console log syntax is correct but this practice is not good by doing 
this we are damaging the readability of code 
code redable hona chahiye
so don't prefer  this practices
*/

// code readability should be high

let name = "shrijeet"
let age = 19
let isLoggedIn = false
let state;

// number -> range is (2 raised to 53) -1
// else if no. is bigger we have bigint named variable
// string can be in ' ' or " "
// boolean == value canbe either true or false
// null is actually an standalone value
// undefined is something you have just given a name and didn't defined its value declared variable but not given value
// symbol 



// object

console.log(typeof false );
